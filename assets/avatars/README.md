# assets/avatars

Place VRoid-exported `.vrm` files here for backup (e.g. `presenter.vrm`).

**Not in git.** After cloning this private repo:

1. Copy your `.vrm` into this folder (optional backup)
2. Copy/overwrite runtime file:

   `apps/presenter-onair/public/avatar/miko.vrm`

Check each model’s license before redistributing outside your private use.
