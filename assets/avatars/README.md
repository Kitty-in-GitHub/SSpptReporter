# assets/avatars

本地 VRM 归档（**不进 Git**）。

| 文件 | 说明 |
|------|------|
| `StarString1.0.vrm` | 当前使用的皮套（主模型） |
| `miko.vrm` | OnAir 默认示例模型备份 |

**运行时路径**（应用实际加载）：

`apps/presenter-onair/public/avatar/StarString1.0.vrm`

换机后把对应 `.vrm` 复制到上述 `public/avatar/` 即可。注意各模型授权条款。
