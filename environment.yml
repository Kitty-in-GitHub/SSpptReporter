# conda environment for SSreporter (Node toolchain)
name: ssreporter
channels:
  - conda-forge
  - defaults
dependencies:
  - nodejs=22
  - python=3.11
  - pip
  - pip:
    - -r apps/tts-gateway/requirements.txt
