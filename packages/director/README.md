# @ssreporter/director

Phase 0：DirectorAction 类型 + Ajv 校验 + emotion → VRM 表情映射。

```ts
import { validateDirectorAction, emotionToVrmExpression } from '@ssreporter/director';
```

Schema 真相源：`schemas/director-action.schema.json`。
